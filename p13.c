//prime number 1 to 100 wihout using loop
//square root without using math.h,loop,if,else,operator,sqrt,ternary 
#include<stdio.h>
//#include<math.h>
double pow(double,double);
void main()
{	
	float n;
	printf("Enter the no:");
	scanf("%f",&n);
	printf("swuare root of 25 is %f",pow(n,0.5));
}
