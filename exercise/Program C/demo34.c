// Check whether the given number is multiple of 7 or not. 
#include<stdio.h>
void main()
{
	int num;
	printf("Enter the number to print : ");
	scanf("%d",&num);
	if(num%7==0)
	{
		printf("the value is multiply");
	}
	else
	{
		printf("the value is not multiply");
	}
}
